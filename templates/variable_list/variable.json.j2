[
    {
        "name": "listener_port",
        "value": "{{ listener_port }}"
    },
    {
        "name": "database_size",
        "value": "{{ database_size }}"
    },
    {
        "name": "auto_tune_staging_drive",
        "value": "{{ auto_tune_staging_drive | default('true') }}"
    },
    {
        "name": "allocate_pg_hugepage",
        "value": "{{ allocate_pg_hugepage | default('false') }}"
    },
    {
        "name": "cluster_database",
        "value": "{{ cluster_database | default('false') }}"
    },
    {
        "name": "enable_peer_auth",
        "value": "{{ enable_peer_auth | default('false') }}"
    },
    {
        "name": "ensure_vm_host_distribution",
        "value": "{{ ensure_vm_host_distribution | default('false') }}"
    },
    {
        "name": "dbserver_description",
        "value": "{{ dbserver_description }}"
    },
    {
        "name": "database_names",
        "value": "{{ database_names }}"
    },
    {
        "name": "db_password",
        "value": "{{ db_password }}"
    }
]